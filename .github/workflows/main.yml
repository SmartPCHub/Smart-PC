name: CI

on: [push, workflow_dispatch]

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: Download
      run: Invoke-WebRequest  -OutFile  zrok_0.4.32_windows_amd64.zip
    - name: Extract
      run: Expand-Archive zrok_0.4.32_windows_amd64.zip                
    - name: Directory  Path
      run: cd
    
           
    - name: Zrok version
      run: zrok version
    - name: Create Tunnel
      run: zrok.exe tcp 3389
